# Offline PMTiles

Place generated region archives here for local development.

Expected default:

```text
public/tiles/resilience-demo.pmtiles
```

The app reads `VITE_PMTILES_URL` and defaults to `/tiles/resilience-demo.pmtiles`.
